" upload to a board "

from ..logger import logger

log = logger(__name__)


class Uploader:
    def __init__(self, firmware):
        self.firmware = firmware
        log.critical("Unfinished")

    def upload(self):
        log.critical("Unfinished")
